# Harmonix OS — Encrypted Secrets (Template)
# Encrypt with: sops secrets/secrets.yaml
# Requires age key at: /persist/home/architect/.config/sops/age/keys.txt
#
# Generate age key:
#   mkdir -p /persist/home/architect/.config/sops/age
#   age-keygen -o /persist/home/architect/.config/sops/age/keys.txt
#
# Then update .sops.yaml with the public key and run:
#   sops secrets/secrets.yaml

# PLACEHOLDER — encrypt this file with sops before use
openrouter:
    api_key: "REPLACE_WITH_REAL_KEY"
tailscale:
    auth_key: "REPLACE_WITH_REAL_KEY"
